apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-query-service
  namespace: dfras
  labels:
    app: ai-query-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-query-service
  template:
    metadata:
      labels:
        app: ai-query-service
    spec:
      containers:
      - name: ai-query-service
        image: dfras-infrastructure-ai-query-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8004
        env:
        - name: POSTGRES_HOST
          value: "postgres-service"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_USER
          value: "dfras_user"
        - name: POSTGRES_PASSWORD
          value: "dfras_password"
        - name: POSTGRES_DB
          value: "dfras_db"
        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: ai-query-service
  namespace: dfras
  labels:
    app: ai-query-service
spec:
  selector:
    app: ai-query-service
  ports:
  - port: 8004
    targetPort: 8004
    protocol: TCP
  type: ClusterIP
