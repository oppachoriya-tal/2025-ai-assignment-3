apiVersion: apps/v1
kind: Deployment
metadata:
  name: correlation-service
  namespace: dfras
  labels:
    app: correlation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: correlation-service
  template:
    metadata:
      labels:
        app: correlation-service
    spec:
      containers:
      - name: correlation-service
        image: dfras-infrastructure-correlation-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8009
        env:
        - name: POSTGRES_HOST
          value: "postgres-service"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_USER
          value: "dfras_user"
        - name: POSTGRES_PASSWORD
          value: "dfras_password"
        - name: POSTGRES_DB
          value: "dfras_db"
        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"
        livenessProbe:
          httpGet:
            path: /health
            port: 8009
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8009
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: correlation-service
  namespace: dfras
  labels:
    app: correlation-service
spec:
  selector:
    app: correlation-service
  ports:
  - port: 8009
    targetPort: 8009
    protocol: TCP
  type: ClusterIP
