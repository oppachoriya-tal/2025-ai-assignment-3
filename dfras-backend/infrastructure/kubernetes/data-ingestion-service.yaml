apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-ingestion-service
  namespace: dfras
  labels:
    app: data-ingestion-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: data-ingestion-service
  template:
    metadata:
      labels:
        app: data-ingestion-service
    spec:
      containers:
      - name: data-ingestion-service
        image: dfras-infrastructure-data-ingestion-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8005
        env:
        - name: POSTGRES_HOST
          value: "postgres-service"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_USER
          value: "dfras_user"
        - name: POSTGRES_PASSWORD
          value: "dfras_password"
        - name: POSTGRES_DB
          value: "dfras_db"
        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: data-ingestion-service
  namespace: dfras
  labels:
    app: data-ingestion-service
spec:
  selector:
    app: data-ingestion-service
  ports:
  - port: 8005
    targetPort: 8005
    protocol: TCP
  type: ClusterIP
