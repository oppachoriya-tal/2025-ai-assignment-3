apiVersion: apps/v1
kind: Deployment
metadata:
  name: deep-learning-service
  namespace: dfras
  labels:
    app: deep-learning-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deep-learning-service
  template:
    metadata:
      labels:
        app: deep-learning-service
    spec:
      containers:
      - name: deep-learning-service
        image: dfras-infrastructure-deep-learning-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8011
        env:
        - name: POSTGRES_HOST
          value: "postgres-service"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_USER
          value: "dfras_user"
        - name: POSTGRES_PASSWORD
          value: "dfras_password"
        - name: POSTGRES_DB
          value: "dfras_db"
        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"
        livenessProbe:
          httpGet:
            path: /health
            port: 8011
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8011
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: deep-learning-service
  namespace: dfras
  labels:
    app: deep-learning-service
spec:
  selector:
    app: deep-learning-service
  ports:
  - port: 8011
    targetPort: 8011
    protocol: TCP
  type: ClusterIP
