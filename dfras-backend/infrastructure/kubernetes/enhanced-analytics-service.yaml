apiVersion: apps/v1
kind: Deployment
metadata:
  name: enhanced-analytics-service
  namespace: dfras
  labels:
    app: enhanced-analytics-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: enhanced-analytics-service
  template:
    metadata:
      labels:
        app: enhanced-analytics-service
    spec:
      containers:
      - name: enhanced-analytics-service
        image: dfras-infrastructure-enhanced-analytics-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8010
        env:
        - name: POSTGRES_HOST
          value: "postgres-service"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_USER
          value: "dfras_user"
        - name: POSTGRES_PASSWORD
          value: "dfras_password"
        - name: POSTGRES_DB
          value: "dfras_db"
        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"
        livenessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: enhanced-analytics-service
  namespace: dfras
  labels:
    app: enhanced-analytics-service
spec:
  selector:
    app: enhanced-analytics-service
  ports:
  - port: 8010
    targetPort: 8010
    protocol: TCP
  type: ClusterIP
