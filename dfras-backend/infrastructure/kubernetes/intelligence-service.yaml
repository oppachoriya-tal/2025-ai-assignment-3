apiVersion: apps/v1
kind: Deployment
metadata:
  name: intelligence-service
  namespace: dfras
  labels:
    app: intelligence-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: intelligence-service
  template:
    metadata:
      labels:
        app: intelligence-service
    spec:
      containers:
      - name: intelligence-service
        image: dfras-infrastructure-intelligence-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8006
        env:
        - name: POSTGRES_HOST
          value: "postgres-service"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_USER
          value: "dfras_user"
        - name: POSTGRES_PASSWORD
          value: "dfras_password"
        - name: POSTGRES_DB
          value: "dfras_db"
        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"
        livenessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: intelligence-service
  namespace: dfras
  labels:
    app: intelligence-service
spec:
  selector:
    app: intelligence-service
  ports:
  - port: 8006
    targetPort: 8006
    protocol: TCP
  type: ClusterIP
